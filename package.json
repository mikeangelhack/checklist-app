{
  "name": "checklist-app",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained sample checklist app: vanilla HTML/CSS/JS with no build step, tested by Node's built-in test runner.",
  "type": "module",
  "scripts": {
    "test": "node --test test/"
  }
}
